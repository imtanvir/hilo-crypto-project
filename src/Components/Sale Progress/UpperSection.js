import React from "react";
import styles from "../../Styles/Sale Progress/UpperSection.module.css";

const UpperSection = () => {
  return (
    <div className={`${styles.mainContainer}`}>
      <h1>GET EARLY ACCESS TO HIlo PRESALE. REGISTER BELOW FOR HELP BUYING</h1>
      <p>
        The all New Hilo, Game is a multiplayer online crypto game that is
        played across the chain. It is high security HILO makes sure everybody
        can play safely. Hilo game project is full of potential. The project can
        be a huge success in upcoming years. Hilo game project can offer a
        secure, transparent, and decentralized way to play games with stable
        coin(USDT). Purchase the $hilo token now!
      </p>
    </div>
  );
};

export default UpperSection;
